
class  MyPrint { // class "MyPrint"
 String x = "Hello!";
    public void myMethod (String y){ // new method "myMethod"
                                     // A method is a block of code which only runs when it is called. 
                                     //  You can pass data, known as parameters, into a method. 
                                     // Methods are used to perform certain actions, and they are also known as functions.
                                     // In this case, "myMethod" method does not have a return value as it is "void", and accepts string inputs (parameters);
            System.out.println(y);} 
 }
public class Main { // main class
    public static void main(String[] args)  
      { MyPrint myObj = new MyPrint();
         System.out.println(myObj.x);
         myObj.myMethod("Bye!"); // executes "myMethod" method from "myObj" object with "Bye!" input
      }
}
